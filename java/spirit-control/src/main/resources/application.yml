spring:
  application:
    name: spirit-control
  datasource:
    url: jdbc:postgresql://localhost:5432/spirit_control_db
    username: postgres
    password: CrashLoyale
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    properties:
      hibernate:
        format_sql: true
